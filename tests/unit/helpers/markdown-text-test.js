import {
  markdownText
} from 'bartleby/helpers/markdown-text';

module('MarkdownTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = markdownText(42);
  ok(result);
});
